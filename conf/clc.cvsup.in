#
# /etc/ports/clc.cvsup: CLC ports (http:/crux.fh-regensburg.de/)
#

# Default settings
*default host=clc.morpheus.net
*default release=cvs
*default delete
*default use-rel-suffix
*default compress
*default base=/usr/ports
*default prefix=/usr/ports

# Official contrib ports
contrib tag=CONTRIB-#CRUX_VERSION#
unmaintained tag=UNMAINTAINED

# End of file
