#
# /etc/ports/crux.cvsup: cvsup(1) configuration for CRUX ports system
#

# Default settings
*default host=cvsup.fukt.bth.se
*default base=/usr/ports
*default prefix=/usr/ports
*default release=cvs
*default delete
*default use-rel-suffix
*default compress
*default tag=STABLE-#CRUX_VERSION#

# Port collections
base
opt

# End of file
