No real install procedure yet, to try ilenia build vercmp.so with:

$ python setup.py build

put vercmp.so in ilenia/lib:

$ mv build/lib.linux-i686-2.4/vercmp.so ilenia/lib

go to ilenia/ directory and run ilenia:

$ cd ilenia
$ python ilenia.py -h
